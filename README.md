# KIOSK Agent 
## Getting Started

Follow these steps to set up and run the KIOSK Agent project.

### Installation

1. **Create a Conda environment:**
   ```bash
   conda create -n gui python==3.11 -y
   ```

2. **Activate the environment:**
   ```bash
   conda activate gui
   ```

3. **Install the required dependencies:**
   ```bash
   pip install -r requirements.txt
   ```

### Running the Application
```bash
bash run1.sh
```

> **TODO:** If the project requires environment variables (e.g., API keys), ensure you have created a `.env` file or exported the necessary variables before execution.
